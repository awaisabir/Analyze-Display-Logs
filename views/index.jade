extends layout


block header
  link(rel='stylesheet', href="/stylesheets/uploadfile.css")
  script(src="/javascripts/Chart.js")
  script(src="/javascripts/jquery.form.js")
  script(src="/javascripts/jquery.uploadfile.js")
  script(src="/javascripts/FileSaver.js")
  script(src="/javascripts/account.js")

block content
  h1= title

  p#stats Currently we have #{numFiles} log files to analyze containing #{numEntries} entries.

  h2 Log Query
    //form#queryForm()
  fieldset
      div
        label Message?
        input#message(type="text", name="message")
      div
        label Service?
        input#service(type="text", name="service")
      div
        label File?
        input#file(type="text", name="file")
      div
        label Month?
        input#month(type="text", name="month")
      div
        label Day?
        input#day(type="text", name="day")
      div
        label Output Type:
        select(id="queryType", name="queryType")
          option(value="show", selected) Show
          option(value="visualize") Visualize
          option(value="download") Download
    //br
    //button#submitQuery(type="submit") Submit Log Query

  div
    button#button Submit

  h2 Files uploaded:

  div
   ul#fileList

  div#fileuploader


  textarea#box(rows="40" cols="75")

  div
    canvas(id="myChart", width="400", height="400")
